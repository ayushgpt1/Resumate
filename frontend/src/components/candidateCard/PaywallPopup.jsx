// Paywall removed - everything is free now.
// This component is kept as a stub to avoid breaking imports,
// but it renders nothing.

const PaywallPopup = () => {
  return null;
};

export default PaywallPopup;